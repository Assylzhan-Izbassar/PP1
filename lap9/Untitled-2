#include <iostream>
#include <queue>

using namespace std;

int main(){

    map<string,int> reg;
    int n;
    cin >> n;
    string s;
    for(int i = 0;i < n;i++){
        cin >> s;
        m[s]++;
    }

    map<string,int>::iterator it;
    for(it= reg.begin();it != req.end();it++){
        cout << (*it).first << " " << (*it).second << ednls;
    }



    return 0;
}