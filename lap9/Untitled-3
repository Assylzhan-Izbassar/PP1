#include <iostream>
#include <queue>

using namespace std;

int main(){
    string x;
    map<string,int> reg;
    while(cin >> x){
        req[x]++;
    }
    int cnt = 0;
    map<string,int>::iterator it;
    for(it= reg.begin();it != req.end();it++){
        cnt++;
    }
    if(cnt >= 7) cout << "Good";
    else cout << "Not good";


    return 0;
}