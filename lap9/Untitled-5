#include <iostream>
#include <queue>

using namespace std;

int main(){
    string x;
    map<string,string> reg;
    int n ;
    cin > n;
    for(int i = 0; i<n;i++){
        string x,g;
        cin>>x>>g;
        req[g] = x;
    }

    int k ;
    cin >> k;
    for(int i = 0;i <k;i++){
        string x;
        cin >> x;
        cout << req[x] << endl;
    }
    return 0;
}