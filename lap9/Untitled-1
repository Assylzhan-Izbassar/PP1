#include <iostream>
#include <queue>

using namespace std;

int main(){

    set<int> reg;

    int n;
    
    while(cin>>n)
        if(n==0) break;
        reg.insert(n);
    }

    set<int>::reversse_iterator it;
    for(it= reg.begin();it != req.end();it++){
        cout << *it;
        break;
    }



    return 0;
}